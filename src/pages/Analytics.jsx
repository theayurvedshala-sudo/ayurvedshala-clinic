import {useEffect,useState} from 'react';import {Printer} from 'lucide-react';import {Bar,BarChart,CartesianGrid,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import api from '../services/api';import {Empty} from '../components/UI';
const months=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];export default function Analytics(){const [year,setYear]=useState(new Date().getFullYear()),[data,setData]=useState(null);useEffect(()=>{let alive=true;(async()=>{const {data}=await api.get('/analytics',{params:{year}});if(alive)setData(data)})();return()=>{alive=false}},[year]);const monthly=months.map((month,i)=>({month,revenue:data?.monthly?.find(x=>x._id===i+1)?.revenue||0,bills:data?.monthly?.find(x=>x._id===i+1)?.bills||0}));return <><div className="page-heading"><div><h1>Reports & Analytics</h1><p>Revenue, patient, medicine and doctor performance intelligence.</p></div><div className="heading-actions"><select value={year} onChange={e=>setYear(Number(e.target.value))}>{Array.from({length:6},(_,i)=>new Date().getFullYear()-i).map(x=><option key={x}>{x}</option>)}</select><button className="btn outline" onClick={()=>window.print()}><Printer size={15}/>Print</button></div></div><div className="analytics-grid"><div className="erp-card pad"><div className="card-title"><div><h3>Monthly revenue</h3><p>Financial performance for {year}</p></div></div><div className="chart-box"><ResponsiveContainer width="100%" height="100%"><BarChart data={monthly}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="month"/><YAxis/><Tooltip/><Bar dataKey="revenue" fill="#059669" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></div><div className="erp-card pad"><div className="card-title"><div><h3>Doctor performance</h3><p>Consultations completed</p></div></div><div className="chart-box"><ResponsiveContainer width="100%" height="100%"><BarChart data={data?.doctorPerformance||[]} layout="vertical"><CartesianGrid strokeDasharray="3 3" horizontal={false}/><XAxis type="number"/><YAxis type="category" dataKey="name" width={90}/><Tooltip/><Bar dataKey="consultations" fill="#2563eb" radius={[0,6,6,0]}/></BarChart></ResponsiveContainer></div></div><div className="erp-card pad span-all"><div className="card-title"><h3>Top medicine usage</h3></div><div className="table-wrap"><table><thead><tr><th>Medicine</th><th className="right">Dispensed quantity</th></tr></thead><tbody>{data?.topMedicines?.map(x=><tr key={x.name}><td><strong>{x.name}</strong></td><td className="right"><strong>{x.quantity}</strong></td></tr>)}</tbody></table>{!data?.topMedicines?.length&&<Empty>No medicine usage data for this year.</Empty>}</div></div></div></>}
